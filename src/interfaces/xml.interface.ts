export type XmlInfo = {
    id: string; 
    file: File; 
    name: string; 
    itemsCount: number
};

export type XmlIndirectProduct = {
    id: number;
    ncm: string; 
    unit_measure: string; 
    name: string;
    idFile: string;
};